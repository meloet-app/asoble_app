

import 'package:flutter/material.dart';

class invitePartyPage extends StatelessWidget{

  @override
  Widget build(BuildContext context) {
    return
      new Scaffold(
        appBar: AppBar(),
        body: Center(child: Text("invite")),
      );
  }
}